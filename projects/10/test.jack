/* test */
class Foo {
      static int x, y, z;
      /* comment */
      field char u, v, w;

      function void foo(int foo)
      {
        var int foo, bar;
        var bool b;
        let foo = -1;
        let b = ~false;
        let bar = 3 + foo - (7 + 2) - 5;
        let foo = bar[7+3];
      }
}