class foo{
    static int x,y,z;
    /* comment */
    static char c;
    static void p;
}
/* test */
class Foo {
      static int x, y, z;
      /* comment */
      field char u, v, w;

      function void foo(int foo)
      {
        var int foo, bar;
        var bool b;
        let foo = -1;
        let b = ~false;
        let bar = foo;
      }
}

